@model List<KarnelTravels.Models.HRViewModel>
@{
    ViewBag.Title = "FormCkeditor";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<h1>Restaurant List</h1>

<form>
    <button asp-action="CreateHotel" asp-controller="Admin" class="btn btn-primary">Create Hotel</button>
    <table class="table table-responsive table-hover">
        <thead>
            <tr class="table active">
                <th>Name</th>
                <th>Location</th>
                <th>Price</th>
                <th>Description</th>
                <th>Status</th>
                <th>Image</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                @foreach (var hotel in Model)
                {
                    <tr>
                        <td>@hotel.Name</td>
                        <td>@hotel.SpotName</td>
                        <td>@hotel.Price</td>
                        <td>@hotel.Description</td>
                        <td>@hotel.Status</td>
                        <td><img src="~/img/Hotel_Restaurant/@hotel.ImageUrl" alt="Hotel Image" style="width: 100px; height: 100px;" /></td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <a asp-controller="Admin" asp-action="EditHotel" asp-route-id="@hotel.HrId" class="btn btn-primary">Edit</a>
                                <form asp-controller="Admin" asp-action="DeleteHotel" method="post">
                                    <input type="hidden" name="id" value="@hotel.HrId" />
                                    <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this hotel?')">Delete</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="5">
                        <div>
                            No Hotel available at this moment
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</form>